server {
    listen 8080;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # React Router用のSPAルーティング設定
    location / {
        try_files $uri $uri/ /index.html;
    }
}
